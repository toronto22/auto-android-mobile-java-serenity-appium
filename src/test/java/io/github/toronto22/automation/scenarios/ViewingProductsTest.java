package io.github.toronto22.automation.scenarios;

import net.serenitybdd.junit.runners.SerenityRunner;
import org.junit.Test;
import org.junit.jupiter.api.DisplayName;
import org.junit.runner.RunWith;

@RunWith(SerenityRunner.class)
public class ViewingProductsTest extends BaseTest {
    @Test
    @DisplayName("Customer view products to buy")
    public void customerViewProductsToBuy() {
//        toronto.attemptsTo(Login.with(validUser));
    }

    @Test
    @DisplayName("Customer view products to buy by order")
    public void customerViewProductsToBuyByOrder() {
//        toronto.attemptsTo(Login.with(validUser));
    }

    @Test
    @DisplayName("Customer add products to cart")
    public void customerAddProductsToCart() {
//        toronto.attemptsTo(Login.with(validUser));
    }


}
